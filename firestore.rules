rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Documento: menu/platos
    match /menu/{docId} {
      allow read: if true; // Público de solo lectura
      allow write: if request.auth != null && request.auth.token.email == "rayrayllander@gmail.com";
    }
  }
}
